import { AllowPatternsDirective } from './allow-patterns.directive';

describe('AllowPatternsDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowPatternsDirective();
    expect(directive).toBeTruthy();
  });
});
